---
interface Props {
  title?: string;
}

const { title = 'Details are still being checked' } = Astro.props;
---

<aside class="site-notice" aria-label="Page status">
  <strong>{title}</strong>
  <p><slot /></p>
</aside>
